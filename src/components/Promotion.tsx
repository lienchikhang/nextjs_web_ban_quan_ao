import React from 'react'
import styles from '@/styles/header.module.scss'

const Promotion = () => {
    return (
        <section className={styles.promotion}>
            <p className={styles.paraph}>Nhập COOL50K giảm 50K cho đơn từ 590K</p>
        </section>
    )
}

export default Promotion