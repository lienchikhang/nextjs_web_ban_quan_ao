import React from 'react'
import MenuIcon from '@mui/icons-material/Menu';
import styles from '@/styles/header.module.scss';

const HeaderMenu = () => {
    return (
        <MenuIcon className={styles.headerIconMenu} />
    )
}

export default HeaderMenu